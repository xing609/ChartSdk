<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <corners
        android:bottomLeftRadius="@dimen/d40"
        android:bottomRightRadius="0dp"
        android:topLeftRadius="@dimen/d40"
        android:topRightRadius="0dp" />

    <!-- 形状的填充色 -->
    <solid android:color="@color/c1E3A65" />
    <!-- 边框宽度和颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/c1E3A65" />
</shape>