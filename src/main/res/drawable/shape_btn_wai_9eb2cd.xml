<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="@dimen/d40" />

    <!-- 形状的填充色 -->
    <solid android:color="@color/transparent" />
    <!-- 边框宽度和颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/c9EB2CD" />
</shape>